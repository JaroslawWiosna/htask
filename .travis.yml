dist: trusty
os: linux
language: haskell
sudo: required

compiler:
    - ghc

addons:
    apt:
        packages:
            - ghc

before_script:
    - echo "Before build"

script: 
    - ./build.sh
    - ./htask

after_success:
    - echo "SUCCESS!"

    